--- #roles/{{ role_name }}/meta/main.yml

dependencies:

  - role: ensure_dirs
    tags: ['{{ role_name }}', 'ensure_dirs']
    ensure_dirs_on_remote: '{{ '{{' }} {{ role_name }}_ensure_dirs_on_remote {{ '}}' }}'
    ensure_dirs_on_local : '{{ '{{' }} {{ role_name }}_ensure_dirs_on_local {{ '}}' }}'

#  - role: skel
#    become: true
#    tags: ['minc', 'skel']
#    skel_entries: '{{ '{{' }} {{ role_name }}_skel_entries {{ '}}' }}'

galaxy_info:
  author: 'Christopher Steel'
  description: '{{ role_name }} is used to...'
  company: 'McGill Centre for Integrative Neuroscience'
  license: MIT
  min_ansible_version: '1.7.1'

  platforms:
    - name: Ubuntu
      versions:
        - xenial
#    - name: Debian
#      versions:
#        - wheezy
#        - jessie

  galaxy_tags:
    - mcin
    - {{ role_name }}
    - package
    # NOTE: A tag is limited to a single word comprised of
    # alphanumeric characters. Maximum 20 tags per role.

